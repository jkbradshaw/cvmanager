%h1.content-title= "Add #{@user.name} to faculty?"

%hr
- form_for [:admin,@user,@faculty] do |f|
  <strong>Section: </strong>
  =f.select(:section_id, @sections)
  %hr
  =fg_submit_button "add"
  =fg_cancel_button "cancel"
