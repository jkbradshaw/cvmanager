.publication
  %span.title= "#{paper.title}"
  %br
  %span.journal
    = "#{paper.journal.short_title}, #{paper.journal_volume}(#{paper.journal_issue}):#{paper.journal_pages}"
    = paper.pmed_date
  %br
  %span.authors= html_authors_for(paper,@cv)