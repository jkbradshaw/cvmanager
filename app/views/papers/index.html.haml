- content_for :actions do
  = circle_plus_left_fg_button_to "add new paper", new_cv_paper_path(@cv)

- unless @papers.nil?
  - @papers.each do |p|
    = render :partial=>'paper', :object=>p
    .links.clearfix
      = link_to "remove", delete_cv_paper_path(@cv, p) 
    %hr
      



